---
const ctaHref = "/apply";
---

<section class="relative overflow-hidden bg-slate-950 text-white">
  <!-- Background decorations -->
  <div aria-hidden="true" class="pointer-events-none absolute inset-0">
    <div class="absolute -top-40 left-1/2 h-[520px] w-[520px] -translate-x-1/2 rounded-full blur-3xl opacity-35
                bg-[radial-gradient(circle_at_30%_30%,rgba(255,126,0,.9),transparent_60%)]"></div>

    <div class="absolute -bottom-48 left-10 h-[560px] w-[560px] rounded-full blur-3xl opacity-30
                bg-[radial-gradient(circle_at_30%_30%,rgba(255,0,122,.9),transparent_60%)]"></div>

    <div class="absolute top-20 -right-40 h-[520px] w-[520px] rounded-full blur-3xl opacity-30
                bg-[radial-gradient(circle_at_30%_30%,rgba(168,0,255,.9),transparent_60%)]"></div>

    <!-- subtle grid -->
    <div class="absolute inset-0 opacity-[0.10] bg-[linear-gradient(to_right,rgba(255,255,255,.08)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,.08)_1px,transparent_1px)]
                bg-[size:56px_56px]"></div>
  </div>

  <div class="relative px-4 sm:px-8 lg:px-16 xl:px-24 pt-6 pb-16 lg:pb-24">
    <div class="container mx-auto max-w-7xl grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">

      <!-- Left -->
      <div class="lg:col-span-6">
        

        <h1 class="mt-6 text-4xl sm:text-5xl lg:text-6xl font-semibold leading-[1.05] tracking-tight">
          Build a website that
          <span class="block bg-clip-text text-transparent
                       bg-[linear-gradient(90deg,#ff7e00,#ff007a,#a800ff)]">
            converts — faster.
          </span>
        </h1>

        <p class="mt-6 text-lg text-slate-300 leading-relaxed max-w-xl">
          Most businesses don’t lose customers because of bad ads.
          They lose them because the website is slow, unclear, or tracking is broken.
          We build a code-based growth asset: speed, CRO structure, and GA4 done right.
        </p>

        <div class="mt-10 flex flex-col sm:flex-row gap-4">
          <a
            href={ctaHref}
            class="inline-flex items-center justify-center px-7 py-3.5 rounded-xl font-medium text-white
                   bg-[linear-gradient(135deg,rgba(255,126,0,.95),rgba(255,0,122,.95),rgba(168,0,255,.95))]
                   hover:opacity-95 transition"
          >
            Book Free Customer Leak Scan →
          </a>

          <a
            href="/process"
            class="inline-flex items-center justify-center px-7 py-3.5 rounded-xl font-medium
                   border border-slate-700 text-white hover:border-white transition bg-slate-900/20"
          >
            See the process
          </a>
        </div>

        <div class="mt-10 grid grid-cols-1 sm:grid-cols-3 gap-4 text-sm text-slate-300">
          <div class="rounded-xl border border-slate-800/70 bg-slate-900/25 p-4">
            <p class="font-medium">Speed upgrade</p>
            <p class="text-slate-400 mt-1">We’ve taken sites from ~20 to ~90+.</p>
          </div>
          <div class="rounded-xl border border-slate-800/70 bg-slate-900/25 p-4">
            <p class="font-medium">Tracking that works</p>
            <p class="text-slate-400 mt-1">GA4 events aligned to leads + sales.</p>
          </div>
          <div class="rounded-xl border border-slate-800/70 bg-slate-900/25 p-4">
            <p class="font-medium">Ownership included</p>
            <p class="text-slate-400 mt-1">Code + hosting access + no lock-in.</p>
          </div>
        </div>
      </div>

      <!-- Right -->
      <div class="lg:col-span-6">
        <div class="relative rounded-2xl border border-slate-800/70 bg-slate-900/25 overflow-hidden">
          <!-- top bar -->
          <div class="flex items-center gap-2 px-5 py-4 border-b border-slate-800/70 bg-slate-950/30">
            <span class="h-2.5 w-2.5 rounded-full bg-slate-600"></span>
            <span class="h-2.5 w-2.5 rounded-full bg-slate-600"></span>
            <span class="h-2.5 w-2.5 rounded-full bg-slate-600"></span>
            <div class="ml-3 text-xs text-slate-400">Live Growth Build Preview</div>
          </div>

          <!-- mock content -->
          <div class="p-6 sm:p-8">
            <div class="grid grid-cols-12 gap-4 items-start">
              <div class="col-span-7">
                <div class="h-4 w-2/3 rounded bg-slate-800/70"></div>
                <div class="mt-3 h-3 w-full rounded bg-slate-800/50"></div>
                <div class="mt-2 h-3 w-11/12 rounded bg-slate-800/50"></div>
                <div class="mt-2 h-3 w-10/12 rounded bg-slate-800/50"></div>

                <div class="mt-6 rounded-xl border border-slate-800/70 bg-slate-950/30 p-4">
                  <p class="text-sm font-medium text-white">Leak Scan Output</p>
                  <ul class="mt-3 space-y-2 text-sm text-slate-300">
                    <li class="flex gap-2"><span class="text-slate-400">•</span> Where users drop off</li>
                    <li class="flex gap-2"><span class="text-slate-400">•</span> Tracking gaps & misfires</li>
                    <li class="flex gap-2"><span class="text-slate-400">•</span> Fix roadmap (Go/No-Go)</li>
                  </ul>
                </div>
              </div>

              <div class="col-span-5">
                <div class="rounded-xl border border-slate-800/70 bg-slate-950/30 p-4">
                  <p class="text-xs text-slate-400">Core Web Vitals Targets</p>
                  <div class="mt-3 space-y-3">
                    <div class="flex items-center justify-between text-sm">
                      <span class="text-slate-300">LCP</span>
                      <span class="font-medium bg-clip-text text-transparent bg-[linear-gradient(90deg,#ff7e00,#ff007a,#a800ff)]">&lt; 2.5s</span>
                    </div>
                    <div class="flex items-center justify-between text-sm">
                      <span class="text-slate-300">CLS</span>
                      <span class="font-medium bg-clip-text text-transparent bg-[linear-gradient(90deg,#ff7e00,#ff007a,#a800ff)]">&lt; 0.1</span>
                    </div>
                    <div class="flex items-center justify-between text-sm">
                      <span class="text-slate-300">FCP</span>
                      <span class="font-medium bg-clip-text text-transparent bg-[linear-gradient(90deg,#ff7e00,#ff007a,#a800ff)]">&lt; 1.8s</span>
                    </div>
                  </div>
                </div>

                <div class="mt-4 rounded-xl border border-slate-800/70 bg-slate-950/30 p-4">
                  <p class="text-xs text-slate-400">Guarantees</p>
                  <p class="mt-2 text-sm text-slate-300">
                    Deadlines matter. If we miss due to execution → refunds apply.
                  </p>
                </div>
              </div>
            </div>
          </div>

          <!-- floating deco -->
          <svg aria-hidden="true" class="absolute -bottom-12 -right-10 w-72 h-72  z-[-20] opacity-25 blur-2xl" viewBox="0 0 200 200">
            <defs>
              <linearGradient id="bb_grad" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0" stop-color="#ff7e00" stop-opacity="1"/>
                <stop offset="0.55" stop-color="#ff007a" stop-opacity="1"/>
                <stop offset="1" stop-color="#a800ff" stop-opacity="1"/>
              </linearGradient>
            </defs>
            <path fill="url(#bb_grad)" d="M42.8,-61.3C55.8,-56.1,66.6,-46.7,71.8,-35.2C77,-23.7,76.6,-10.1,74.1,2.6C71.5,15.3,66.8,27.1,59.3,36.7C51.8,46.4,41.4,53.9,30.2,60.9C18.9,67.9,6.8,74.4,-6.6,75.8C-20,77.1,-34.7,73.2,-45.4,64.2C-56.1,55.3,-62.8,41.3,-67.1,27.3C-71.4,13.3,-73.2,-0.7,-70.1,-13.4C-67.1,-26.1,-59.3,-37.5,-49.2,-44.7C-39.2,-51.9,-26.9,-54.9,-14.5,-60.4C-2.1,-65.9,10.4,-73.8,22.6,-72.2C34.8,-70.6,46.6,-59.7,42.8,-61.3Z" transform="translate(100 100)"/>
          </svg>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  import { animate } from "@motionone/dom";

  // simple entrance animations (fast + lightweight)
  document.addEventListener("DOMContentLoaded", () => {
    const hero = document.querySelector("section");
    if (!hero) return;

    const left = hero.querySelector(".lg\\:col-span-6");
    const right = hero.querySelectorAll(".lg\\:col-span-6")[1];

    if (left) animate(left, { opacity: [0, 1], transform: ["translateY(14px)", "translateY(0px)"] }, { duration: .7, easing: "ease-out" });
    if (right) animate(right, { opacity: [0, 1], transform: ["translateY(18px)", "translateY(0px)"] }, { duration: .8, delay: .08, easing: "ease-out" });
  });
</script>
