---
const items = [
  {
    href: '/web-design',
    title: 'Web Design & Development',
    desc: 'Fast, accessible and analytics-ready websites with CRO-first UX.',
    bullets: ['Mobile-first', 'Lighthouse ≥90 target', 'GA4 events', 'SEO-ready'],
    grad: 'from-violet-500 to-fuchsia-500',
    icon: `<svg class="w-7 h-7" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5h18M3 9h18M7 9v10m5-10v10m5-10v10"/></svg>`
  },
  {
    href: '/branding',
    title: 'Branding & Identity',
    desc: 'Identity systems and messaging that lift perceived quality and trust.',
    bullets: ['Logo & variants', 'Color/Type tokens', 'Voice & tone', 'Social kit'],
    grad: 'from-fuchsia-500 to-indigo-500',
    icon: `<svg class="w-7 h-7" viewBox="0 0 24 24" fill="none" stroke="currentColor"><circle cx="12" cy="12" r="9" stroke-width="2"></circle><path stroke-width="2" d="M8 12h8M12 8v8"/></svg>`
  },
  {
    href: '/local-seo',
    title: 'Local SEO (On-Page)',
    desc: 'Technical + on-page SEO and GBP optimization to capture high-intent searches.',
    bullets: ['LocalBusiness schema', 'Internal links', 'GBP setup', 'FAQ/Breadcrumb schema'],
    grad: 'from-indigo-500 to-violet-500',
    icon: `<svg class="w-7 h-7" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.5-4.5M10 18a8 8 0 100-16 8 8 0 000 16z"/></svg>`
  }
];
---

<section class="py-16 lg:py-24 bg-slate-950">
  <div class="container mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl lg:text-4xl font-bold text-white">What we do</h2>
      <p class="text-slate-400 mt-2">Brand • Web • Local SEO — made to sell.</p>
    </div>

    <div class="grid md:grid-cols-3 gap-6">
      {items.map((s, i) => (
        <a href={s.href}
           class="group relative overflow-hidden rounded-2xl border border-slate-800 bg-white/5 p-6 hover:border-violet-500/40 hover:bg-violet-500/10 transition"
           style={`animation: fade-in .6s both; animation-delay:${i*120}ms`}>
          <!-- subtle corner glow -->
          <div class="pointer-events-none absolute -top-24 -right-24 h-56 w-56 rounded-full bg-gradient-to-br from-violet-500/20 to-fuchsia-500/20 blur-2xl"></div>

          <div class={`inline-flex items-center justify-center w-14 h-14 rounded-xl bg-gradient-to-r ${s.grad} text-white mb-4`}>
            <Fragment set:html={s.icon} />
          </div>

          <h3 class="text-white text-xl font-semibold">{s.title}</h3>
          <p class="text-slate-300 mt-2">{s.desc}</p>

          <ul class="mt-4 grid grid-cols-2 gap-2">
            {s.bullets.map((b) => (
              <li class="text-slate-300 text-sm inline-flex items-center gap-2">
                <svg class="w-4 h-4 text-violet-300" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                {b}
              </li>
            ))}
          </ul>

          <span class="mt-5 inline-flex items-center gap-2 text-violet-300">
            Explore
            <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4-4 4M21 12H3"/>
            </svg>
          </span>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
@keyframes fade-in { from{opacity:0;transform:translateY(8px)} to{opacity:1;transform:none} }
</style>
