---
const steps = [
  { n:'01', t:'Discovery', d:'Goals, offers/AOV, locations, capacity and quick opportunities.' },
  { n:'02', t:'Quick Audit', d:'CWV, accessibility, on-page/GBP, analytics sanity — quick wins first.' },
  { n:'03', t:'Scope & Plan', d:'One to three options with KPIs, timeline and trade-offs.' },
  { n:'04', t:'Build & QA', d:'CRO-first templates, schema & events; performance and AA checks.' },
  { n:'05', t:'Launch & Handover', d:'DNS/SSL, analytics, redirects (if any) and owner training.' },
  { n:'06', t:'Measure & Iterate', d:'Warranty window, GSC/GA4 watch; Care Plan optional.' },
];
---

<section class="py-20 bg-slate-950">
  <div class="container mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl lg:text-4xl font-bold text-white">How engagements run</h2>
      <p class="text-slate-400 mt-2">Transparent steps from kickoff to momentum.</p>
    </div>

    <div class="grid lg:grid-cols-3 gap-6">
      {steps.map((s, i) => (
        <div class="rounded-2xl border border-slate-800 bg-white/5 p-6"
             style={`animation: fade-in .6s both; animation-delay:${i*80}ms`}>
          <div class="w-10 h-10 rounded-lg bg-violet-500/20 border border-violet-500/30 text-violet-200 font-bold flex items-center justify-center mb-3">{s.n}</div>
          <h3 class="text-white font-semibold">{s.t}</h3>
          <p class="text-slate-300 mt-1">{s.d}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
@keyframes fade-in {from{opacity:0;transform:translateY(8px)} to{opacity:1;transform:none}}
</style>
