<section class="bg-white py-12">
<script>
function gtag_report_conversion(url) {
  var callback = function () { if (typeof(url) != 'undefined') { window.location = url; } };
  gtag('event', 'conversion', { 'send_to': 'AW-17710658719/wq67CJeHgskbEJ_pjP1B', 'transaction_id': '', 'event_callback': callback });
  return false;
}
</script>
  <div class="container mx-auto px-6 max-w-5xl grid lg:grid-cols-2 gap-14 items-start">

    <div>
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-md bg-orange-50 text-orange-700 text-sm font-medium mb-6">
        Tier 1 — High-Conversion System
      </div>
      <h1 class="text-4xl font-bold text-gray-900 leading-tight">
        A website that converts, ads that bring leads, and a system that never lets a call slip through.
      </h1>
      <p class="mt-5 text-lg text-gray-600 leading-relaxed">
        One flat monthly fee covers the build, the ads, and the lead recovery — so you stop guessing and start seeing real numbers.
      </p>

      <div class="mt-8 space-y-4">
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-green-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          <span class="text-gray-700">High-conversion website, live in 2–3 weeks</span>
        </div>
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-green-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          <span class="text-gray-700">Google or Meta Ads, managed for you</span>
        </div>
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-green-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          <span class="text-gray-700">Dedicated tracked number + missed-call SMS recovery</span>
        </div>
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-green-600 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          <span class="text-gray-700 font-medium">Monthly reporting: leads, cost per lead, source, conversions</span>
        </div>
      </div>

      <p class="mt-6 text-sm text-gray-500">$1,500/mo flat · Ad spend billed directly to you ($500–$1,500/mo recommended) · Month-to-month</p>
    </div>

    <div class="bg-gray-50 border border-gray-200 rounded-xl overflow-hidden">
      <div class="bg-gray-900 text-white px-6 py-4">
        <p class="font-semibold">See if you're a fit</p>
        <p class="text-sm text-gray-400">Takes 30 seconds. We respond within 12 hours.</p>
      </div>
      <form name="tier1-qualification" method="POST" action="/thank-you" data-netlify="true" netlify-honeypot="bot-field" class="p-6 space-y-4">
        <input type="hidden" name="form-name" value="tier1-qualification" />
        <p class="hidden"><input name="bot-field" /></p>
        <input name="name" required placeholder="Your name" class="w-full bg-white border border-gray-300 px-4 py-3 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
        <input name="email" type="email" required placeholder="Email" class="w-full bg-white border border-gray-300 px-4 py-3 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
        <input name="phone" type="tel" required placeholder="Phone" class="w-full bg-white border border-gray-300 px-4 py-3 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
        <input name="business" required placeholder="Business name" class="w-full bg-white border border-gray-300 px-4 py-3 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
        
        <input name="website" placeholder="Current website (optional)" class="w-full bg-white border border-gray-300 px-4 py-3 text-gray-900 rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
        <button type="submit" class="w-full px-6 py-3.5 rounded-lg font-semibold text-white bg-orange-600 hover:bg-orange-700 transition" onclick="return gtag_report_conversion();">
          Get Started →
        </button>
        <p class="text-xs text-gray-500 text-center">Best fit for businesses ready to invest $2,000+/mo total (service fee + ad spend).</p>
      </form>
    </div>

  </div>
</section>
