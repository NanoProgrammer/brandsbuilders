<section class="relative bg-slate-950 text-white overflow-hidden border-b border-slate-800">

<script>
function gtag_report_conversion(url) {
  var callback = function () {
    if (typeof(url) != 'undefined') {
      window.location = url;
    }
  };
  gtag('event', 'conversion', {
      'send_to': 'AW-17710658719/wq67CJeHgskbEJ_pjP1B',
      'transaction_id': '',
      'event_callback': callback
  });
  return false;
}
</script>

  <!-- ambient glow -->
  <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-slate-950 to-fuchsia-950/30"></div>
  <div class="absolute -top-40 right-[-120px] w-[600px] h-[600px] rounded-full bg-orange-500/15 blur-[140px]"></div>

  <div class="relative container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-16 items-center py-12">

    <!-- LEFT TEXT -->
    <div class="reveal">

      <p class="text-sm uppercase tracking-widest text-slate-400 mb-5">
        Rebooking & Reactivation System
      </p>

      <h1 class="text-5xl font-semibold leading-tight">
        Turn past clients into  
        <span class="text-fuchsia-300">new bookings</span>.
      </h1>

      <p class="mt-6 text-xl text-slate-300 leading-relaxed max-w-xl">
        We build your reactivation and rebooking flows —
        email, SMS, scripts and campaigns —
        done for you, based on real customer language.
      </p>

    

    </div>


    <!-- RIGHT : FORM -->
    <div class="relative reveal">

      <!-- glow frame -->
      <div class="absolute -inset-4 bg-gradient-to-br from-orange-500/20 to-fuchsia-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl bg-slate-900/60 overflow-hidden">

        <form
          name="rebooking-system-qualification"
          method="POST"
          action="/thank-you"
          data-netlify="true"
          netlify-honeypot="bot-field"
          class="p-6 space-y-4"
        >
          <input type="hidden" name="form-name" value="rebooking-system-qualification" />
          <p class="hidden"><input name="bot-field" /></p>

          <input name="name" required placeholder="Name"
            class="w-full bg-slate-950 border border-slate-700 px-4 py-3 text-white rounded-md" />

          <input name="email" type="email" required placeholder="Email"
            class="w-full bg-slate-950 border border-slate-700 px-4 py-3 text-white rounded-md" />

            <input name="Phone" type="tel" required placeholder="Phone"
            class="w-full bg-slate-950 border border-slate-700 px-4 py-3 text-white rounded-md" />

          <input name="business" required placeholder="Business name"
            class="w-full bg-slate-950 border border-slate-700 px-4 py-3 text-white rounded-md" />
          

          <input name="website" placeholder="Current website (if any)"
            class="w-full bg-slate-950 border border-slate-700 px-4 py-3 text-white rounded-md" />

          <button type="submit"
            class="w-full inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold
                   bg-gradient-to-r from-orange-500 via-pink-500 to-fuchsia-600
                   shadow-lg shadow-fuchsia-600/30
                   hover:translate-y-[-1px] transition"
            onclick="return gtag_report_conversion();"
          >
            Check qualification →
          </button>

          <p class="text-xs text-slate-500 text-center pt-2">
            Reply within 1 business day.
          </p>

        </form>
      </div>

   
    </div>

  </div>
</section>
