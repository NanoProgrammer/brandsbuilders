<section id="intake-section" class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-16 items-start">

    <!-- LEFT INFO -->
    <div class="reveal">
      <h2 class="text-3xl font-semibold text-white mb-4">
        What happens in the scan
      </h2>

      <p class="text-slate-400 mb-8">
        We review real signals that determine whether
        your website captures or leaks demand.
      </p>

      <ul class="space-y-6 text-slate-300">

        <li class="flex gap-4">
          <svg class="w-6 h-6 text-fuchsia-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path d="M3 12h18M12 3v18"/>
          </svg>
          <span>Speed & Core Web Vitals</span>
        </li>

        <li class="flex gap-4">
          <svg class="w-6 h-6 text-orange-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path d="M12 2l4 8H8l4-8z"/>
          </svg>
          <span>Conversion routing & friction</span>
        </li>

        <li class="flex gap-4">
          <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path d="M4 4h16v16H4z"/>
          </svg>
          <span>Tracking & attribution clarity</span>
        </li>

        <li class="flex gap-4">
          <svg class="w-6 h-6 text-sky-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path d="M12 6v12M6 12h12"/>
          </svg>
          <span>Ownership & platform risks</span>
        </li>

      </ul>

      <p class="mt-8 text-sm text-slate-500">
        You leave with a written recap and Go / No-Go recommendation.
      </p>
    </div>


    <!-- RIGHT FORM -->
    <form
      name="customer-leak-scan"
      method="POST"
      action="/thank-you"
      data-netlify="true"
      netlify-honeypot="bot-field"
      class="space-y-5 reveal"
    >
      <input type="hidden" name="form-name" value="customer-leak-scan" />
      <p class="hidden"><input name="bot-field" /></p>

      <input name="name" required placeholder="Name"
        class="w-full bg-slate-900 border border-slate-700 px-4 py-3 text-white rounded-md" />

      <input name="email" type="email" required placeholder="Email"
        class="w-full bg-slate-900 border border-slate-700 px-4 py-3 text-white rounded-md" />

      <input name="business" required placeholder="Business name"
        class="w-full bg-slate-900 border border-slate-700 px-4 py-3 text-white rounded-md" />

      <input name="website" required placeholder="Website URL"
        class="w-full bg-slate-900 border border-slate-700 px-4 py-3 text-white rounded-md" />

      <textarea
        name="context"
        rows="4"
        placeholder="Briefly describe what feels broken"
        class="w-full bg-slate-900 border border-slate-700 px-4 py-3 text-white rounded-md"
      ></textarea>

      <button type="submit"
        class="inline-flex items-center justify-center px-8 py-3 rounded-xl font-semibold
               bg-gradient-to-r from-orange-500 via-pink-500 to-fuchsia-600
               shadow-lg shadow-fuchsia-600/30
               hover:translate-y-[-1px] transition">
        Submit application →
      </button>
    </form>

  </div>
</section>
