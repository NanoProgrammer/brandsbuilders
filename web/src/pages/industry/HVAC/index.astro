---
import Layout from '../../../layouts/Layout.astro';

const seo = {
  title: 'Upfront Website Builds for HVAC Companies | BrandsBuilders',
  description:
    'We build HVAC websites upfront so you can see how emergency calls, bookings, and dispatch flow — before committing or scaling ads.',
  canonical: 'https://brandsbuilders.ca/industry/hvac'
};
---

<Layout seo={seo}>

<!-- HERO -->
<section class="relative bg-slate-950 text-white overflow-hidden border-b border-slate-800">

  <!-- cool ambient glow -->
  <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-slate-950 to-sky-950/40"></div>
  <div class="absolute -top-40 right-[-100px] w-[520px] h-[520px] rounded-full bg-sky-500/15 blur-[150px]"></div>

  <div class="relative container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-14 items-center py-24">

    <!-- LEFT -->
    <div class="reveal">

      <span class="inline-block px-3 py-1 rounded-full border border-slate-800 bg-slate-900/50 text-slate-300 text-sm mb-5">
        Industry · HVAC Services
      </span>

      <h1 class="text-5xl font-semibold leading-tight">
        When systems fail,  
        <span class="text-sky-300">customers call now</span>.
      </h1>

      <p class="mt-6 text-xl text-slate-300 leading-relaxed max-w-xl">
        We build HVAC websites upfront —
        optimized for speed, call flow, and urgency —
        so you can see real booking behavior before committing.
      </p>

      <div class="mt-10">
        <a href="/apply"
          class="inline-flex items-center px-10 py-4 rounded-xl font-semibold
                 bg-gradient-to-r from-sky-500 via-cyan-500 to-blue-500
                 shadow-lg shadow-sky-600/30
                 hover:translate-y-[-1px] transition">
          Apply for upfront build →
        </a>
      </div>

      <p class="mt-5 text-sm text-slate-500">
        Built first. You decide after.
      </p>

    </div>

    <!-- RIGHT VISUAL -->
    <div class="relative reveal">

      <div class="absolute -inset-4 bg-gradient-to-br from-sky-500/20 to-cyan-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl overflow-hidden bg-slate-900">
        <img
          src="/industry/hero-hvac.webp"
          alt="HVAC booking website"
          class="w-full object-cover"
          loading="eager"
          decoding="async"
        />
      </div>

      <div class="absolute -bottom-6 -right-2 bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-300 shadow-xl">
        Call Flow · Urgency · Tracking
      </div>

    </div>

  </div>
</section>

<!-- PROBLEMS -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-5xl">

    <h2 class="text-3xl font-semibold text-white mb-10 reveal">
      Where HVAC websites usually lose bookings
    </h2>

    <div class="grid md:grid-cols-2 gap-8 text-slate-300">

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Slow mobile pages that lose emergency traffic.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Visitors leaving before calling.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Missing or unreliable call tracking.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        No visibility into cost per booked job.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Booking or dispatch tools that break when scaling.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Agency-owned sites creating long-term dependency.
      </div>

    </div>
  </div>
</section>

<!-- WHAT WE BUILD -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-16 items-start">

    <div class="reveal">
      <h2 class="text-3xl font-semibold text-white mb-6">
        What we build for HVAC companies
      </h2>

      <p class="text-slate-400 mb-8">
        Not audits. Not reports.
        A system customers can actually use under pressure.
      </p>

      <ul class="space-y-6 text-slate-300">

        <li class="flex gap-4">
          <span class="text-sky-400">▸</span>
          Fast-loading pages for urgent situations
        </li>

        <li class="flex gap-4">
          <span class="text-cyan-400">▸</span>
          Call-first routing with minimal friction
        </li>

        <li class="flex gap-4">
          <span class="text-blue-400">▸</span>
          GA4 + call tracking tied to booked jobs
        </li>

        <li class="flex gap-4">
          <span class="text-emerald-400">▸</span>
          Full ownership of code, domain, and data
        </li>

      </ul>

      <p class="mt-8 text-slate-400">
        You review the build first.
        Then decide whether it’s worth continuing.
      </p>
    </div>

    <div class="relative reveal">
      <div class="absolute -inset-4 bg-gradient-to-br from-sky-500/20 to-cyan-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl p-6 bg-slate-900/60">
        <p class="text-sm text-slate-400 mb-3">
          Example system output
        </p>

        <div class="space-y-3 text-slate-300 text-sm">
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Mobile Speed</span>
            <span class="text-emerald-400">Optimized</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Call Flow</span>
            <span class="text-emerald-400">Low friction</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Tracking</span>
            <span class="text-emerald-400">Verified</span>
          </div>
          <div class="flex justify-between">
            <span>Ownership</span>
            <span class="text-emerald-400">Client-owned</span>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- CTA -->
<section class="relative bg-slate-950 py-28 overflow-hidden text-center">

  <div class="absolute inset-0 bg-gradient-to-r from-sky-500/15 via-cyan-500/15 to-blue-500/15 blur-3xl"></div>

  <div class="relative container mx-auto px-6 max-w-4xl reveal">

    <h2 class="text-3xl lg:text-4xl font-semibold text-white">
      Want to see your HVAC website built?
    </h2>

    <p class="mt-5 text-slate-300 text-lg max-w-2xl mx-auto">
      Apply to have your website built upfront.
      Review the system first. Decide after.
    </p>

    <a
      href="/apply"
      class="inline-flex items-center justify-center mt-8 px-10 py-4 rounded-xl font-semibold
             bg-gradient-to-r from-sky-500 via-cyan-500 to-blue-500
             text-white shadow-lg shadow-sky-600/30
             hover:translate-y-[-1px] hover:shadow-sky-600/40 transition"
    >
      Apply for upfront build →
    </a>

    <p class="mt-5 text-sm text-slate-400">
      No pitch. No pressure. No obligation.
    </p>

  </div>
</section>

</Layout>
