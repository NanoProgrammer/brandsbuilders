---
import Layout from '../../../layouts/Layout.astro';

const seo = {
  title: 'Upfront Website Builds for Dental Clinics | BrandsBuilders',
  description:
    'We build dental clinic websites upfront so you can see how patients flow, book, and convert — before committing or scaling traffic.',
  canonical: 'https://brandsbuilders.ca/industry/dental-clinics'
};
---

<Layout seo={seo}>

<!-- HERO -->
<section class="relative bg-slate-950 text-white overflow-hidden border-b border-slate-800">

  <!-- ambient glow -->
  <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-slate-950 to-fuchsia-950/30"></div>
  <div class="absolute -top-32 right-[-120px] w-[520px] h-[520px] rounded-full bg-orange-500/15 blur-[140px]"></div>

  <div class="relative container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-14 items-center py-24">

    <!-- LEFT -->
    <div class="reveal">

      <span class="inline-block px-3 py-1 rounded-full border border-slate-800 bg-slate-900/50 text-slate-300 text-sm mb-5">
        Industry · Dental Clinics
      </span>

      <h1 class="text-5xl font-semibold leading-tight">
        New patients should  
        <span class="text-fuchsia-300">book appointments</span>,  
        not bounce.
      </h1>

      <p class="mt-6 text-xl text-slate-300 leading-relaxed max-w-xl">
        We build dental clinic websites upfront —
        focused on speed, booking flow, and ownership —
        so you can see real patient conversion before committing.
      </p>

      <div class="mt-10">
        <a href="/apply"
          class="inline-flex items-center px-10 py-4 rounded-xl font-semibold
                 bg-gradient-to-r from-orange-500 via-pink-500 to-fuchsia-600
                 shadow-lg shadow-fuchsia-600/30
                 hover:translate-y-[-1px] transition">
          Apply for upfront build →
        </a>
      </div>

      <p class="mt-5 text-sm text-slate-500">
        Built first. You decide after.
      </p>

    </div>

    <!-- RIGHT VISUAL -->
    <div class="relative reveal">

      <div class="absolute -inset-4 bg-gradient-to-br from-orange-500/20 to-fuchsia-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl overflow-hidden bg-slate-900">
        <img
          src="/industry/dental-hero.webp"
          alt="Dental clinic booking website"
          class="w-full object-cover"
          loading="eager"
          decoding="async"
        />
      </div>

      <div class="absolute -bottom-6 -right-2 bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-300 shadow-xl">
        Speed · Booking Flow · Tracking
      </div>

    </div>

  </div>
</section>

<!-- PROBLEMS -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-5xl">

    <h2 class="text-3xl font-semibold text-white mb-10 reveal">
      Where dental websites usually fail
    </h2>

    <div class="grid md:grid-cols-2 gap-8 text-slate-300">

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Mobile pages that load too slowly for paid traffic.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Visits that never turn into booked appointments.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Missing or unreliable call and booking tracking.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        No visibility into cost per acquired patient.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        CMS or booking tools that limit future scaling.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Agency-owned websites creating long-term dependency.
      </div>

    </div>
  </div>
</section>

<!-- WHAT WE BUILD -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-16 items-start">

    <div class="reveal">
      <h2 class="text-3xl font-semibold text-white mb-6">
        What we build for dental clinics
      </h2>

      <p class="text-slate-400 mb-8">
        Not audits. Not reports.
        A working system patients can actually use.
      </p>

      <ul class="space-y-6 text-slate-300">

        <li class="flex gap-4">
          <span class="text-fuchsia-400">▸</span>
          Fast, mobile-first pages that hold paid traffic
        </li>

        <li class="flex gap-4">
          <span class="text-orange-400">▸</span>
          Booking flows optimized for low friction
        </li>

        <li class="flex gap-4">
          <span class="text-emerald-400">▸</span>
          GA4 + call tracking tied to patient actions
        </li>

        <li class="flex gap-4">
          <span class="text-sky-400">▸</span>
          Full ownership of code, domain, and data
        </li>

      </ul>

      <p class="mt-8 text-slate-400">
        You review the build first.
        Then decide whether it’s worth moving forward.
      </p>
    </div>

    <div class="relative reveal">
      <div class="absolute -inset-4 bg-gradient-to-br from-fuchsia-500/20 to-orange-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl p-6 bg-slate-900/60">
        <p class="text-sm text-slate-400 mb-3">
          Example system output
        </p>

        <div class="space-y-3 text-slate-300 text-sm">
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Mobile Speed</span>
            <span class="text-emerald-400">Optimized</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Booking Flow</span>
            <span class="text-emerald-400">Low friction</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Tracking</span>
            <span class="text-emerald-400">Verified</span>
          </div>
          <div class="flex justify-between">
            <span>Ownership</span>
            <span class="text-emerald-400">Client-owned</span>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- CTA -->
<section class="relative bg-slate-950 py-28 overflow-hidden text-center">

  <div class="absolute inset-0 bg-gradient-to-r from-orange-500/15 via-pink-500/15 to-fuchsia-600/15 blur-3xl"></div>

  <div class="relative container mx-auto px-6 max-w-4xl reveal">

    <h2 class="text-3xl lg:text-4xl font-semibold text-white">
      Want to see your dental website built?
    </h2>

    <p class="mt-5 text-slate-300 text-lg max-w-2xl mx-auto">
      Apply to have your website built upfront.
      Review the system first. Decide after.
    </p>

    <a
      href="/apply"
      class="inline-flex items-center justify-center mt-8 px-10 py-4 rounded-xl font-semibold
             bg-gradient-to-r from-orange-500 via-pink-500 to-fuchsia-600
             text-white shadow-lg shadow-fuchsia-600/30
             hover:translate-y-[-1px] hover:shadow-fuchsia-600/40 transition"
    >
      Apply for upfront build →
    </a>

    <p class="mt-5 text-sm text-slate-400">
      No pitch. No pressure. No obligation.
    </p>

  </div>
</section>

</Layout>
