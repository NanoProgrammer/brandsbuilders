---
import Layout from '../../../layouts/Layout.astro';

const seo = {
  title: 'Med Spa Reactivation & Rebooking System | BrandsBuilders',
  description:
    'Done-for-you rebooking and reactivation system for medical spas to increase repeat treatments, consultation show-up rates, and booked sessions from past clients.',
  canonical: 'https://brandsbuilders.ca/industry/med-spas'
};
---

<Layout seo={seo}>

<!-- HERO -->
<section class="relative bg-slate-950 text-white overflow-hidden border-b border-slate-800">

  <div class="absolute inset-0 bg-gradient-to-br from-slate-950 via-slate-950 to-fuchsia-950/30"></div>
  <div class="absolute -top-32 left-[-120px] w-[520px] h-[520px] rounded-full bg-pink-500/15 blur-[140px]"></div>

  <div class="relative container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-14 items-center py-24">

    <!-- LEFT -->
    <div class="reveal">

      <span class="inline-block px-3 py-1 rounded-full border border-slate-800 bg-slate-900/50 text-slate-300 text-sm mb-5">
        Industry · Medical Spas
      </span>

      <h1 class="text-5xl font-semibold leading-tight">
        More repeat treatments.  
        Higher consultation show-up.  
        <span class="text-fuchsia-300">More booked sessions.</span>
      </h1>

      <p class="mt-6 text-xl text-slate-300 leading-relaxed max-w-xl">
        We install a done-for-you rebooking and client reactivation system
        that turns your past client database into new consultations,
        repeat treatments, and recovered appointment slots.
      </p>

      <div class="mt-10">
        <a href="/apply"
          class="inline-flex items-center px-10 py-4 rounded-xl font-semibold
                 bg-gradient-to-r from-pink-500 via-fuchsia-500 to-orange-500
                 shadow-lg shadow-fuchsia-600/30 hover:translate-y-[-1px] transition">
          Check clinic qualification →
        </a>
      </div>

      <p class="mt-5 text-sm text-slate-500">
        Most early booking gains come from existing clients.
      </p>

    </div>

    <!-- RIGHT -->
    <div class="relative reveal">

      <div class="absolute -inset-4 bg-gradient-to-br from-fuchsia-500/20 to-pink-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl overflow-hidden bg-slate-900">
        <img
          src="/industry/hero-medspa.webp"
          alt="Medical spa consultation and rebooking system"
          class="w-full object-cover"
          loading="eager"
          decoding="async"
        />
      </div>

      <div class="absolute -bottom-6 -right-2 bg-slate-900 border border-slate-800 rounded-xl px-4 py-3 text-sm text-slate-300 shadow-xl">
        Reactivation · Show-Up · Repeat Visits
      </div>

    </div>

  </div>
</section>

<!-- PROBLEMS -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-5xl">

    <h2 class="text-3xl font-semibold text-white mb-10 reveal">
      Where med spa revenue quietly leaks
    </h2>

    <div class="grid md:grid-cols-2 gap-8 text-slate-300">

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Past clients are never contacted after treatments.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Treatment series are not actively rebooked.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Consultation no-shows reduce daily revenue.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Follow-up depends on staff memory.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Cancellation gaps stay empty.
      </div>

      <div class="reveal border border-slate-800 rounded-xl p-6 bg-slate-900/40">
        Client database exists — but is inactive.
      </div>

    </div>
  </div>
</section>

<!-- WHAT WE INSTALL -->
<section class="bg-slate-950 py-24 border-b border-slate-800">
  <div class="container mx-auto px-6 max-w-6xl grid lg:grid-cols-2 gap-16 items-start">

    <div class="reveal">
      <h2 class="text-3xl font-semibold text-white mb-6">
        What we install for medical spas
      </h2>

      <p class="text-slate-400 mb-8">
        Not marketing campaigns.
        A working repeat-booking engine.
      </p>

      <ul class="space-y-6 text-slate-300">

        <li class="flex gap-4">
          <span class="text-fuchsia-400">▸</span>
          Past-client reactivation campaigns
        </li>

        <li class="flex gap-4">
          <span class="text-pink-400">▸</span>
          Treatment series rebooking flows
        </li>

        <li class="flex gap-4">
          <span class="text-orange-400">▸</span>
          Consultation confirmation & show-up sequences
        </li>

        <li class="flex gap-4">
          <span class="text-emerald-400">▸</span>
          Cancellation & empty-slot recovery
        </li>

      </ul>

      <p class="mt-8 text-slate-400">
        Designed to generate measurable bookings
        within the first 60 days.
      </p>
    </div>

    <!-- RIGHT PANEL -->
    <div class="relative reveal">

      <div class="absolute -inset-4 bg-gradient-to-br from-pink-500/20 to-fuchsia-500/20 blur-2xl rounded-3xl"></div>

      <div class="relative border border-slate-800 rounded-2xl p-6 bg-slate-900/60">

        <p class="text-sm text-slate-400 mb-3">
          System impact areas
        </p>

        <div class="space-y-3 text-slate-300 text-sm">
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Repeat treatments</span>
            <span class="text-emerald-400">Increased</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Consultation show-up</span>
            <span class="text-emerald-400">Improved</span>
          </div>
          <div class="flex justify-between border-b border-slate-800 pb-2">
            <span>Series completion</span>
            <span class="text-emerald-400">Higher</span>
          </div>
          <div class="flex justify-between">
            <span>Empty slots</span>
            <span class="text-emerald-400">Recovered</span>
          </div>
        </div>

      </div>
    </div>

  </div>
</section>

<!-- CTA -->
<section class="relative bg-slate-950 py-28 overflow-hidden text-center">

  <div class="absolute inset-0 bg-gradient-to-r from-pink-500/15 via-fuchsia-500/15 to-orange-500/15 blur-3xl"></div>

  <div class="relative container mx-auto px-6 max-w-4xl reveal">

    <h2 class="text-3xl lg:text-4xl font-semibold text-white">
      Want your client database activated?
    </h2>

    <p class="mt-5 text-slate-300 text-lg max-w-2xl mx-auto">
      Check if your medical spa qualifies for the
      rebooking and reactivation system.
    </p>

    <a href="/apply"
       class="inline-flex items-center justify-center mt-8 px-10 py-4 rounded-xl font-semibold
       bg-gradient-to-r from-pink-500 via-fuchsia-500 to-orange-500
       shadow-lg shadow-fuchsia-600/30 hover:translate-y-[-1px] transition">
      Check qualification →
    </a>

    <p class="mt-5 text-sm text-slate-400">
      Fit check only. No pressure.
    </p>

  </div>
</section>

</Layout>
