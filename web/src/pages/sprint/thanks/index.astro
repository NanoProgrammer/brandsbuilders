---
import Layout from "../../../layouts/Layout.astro";

const siteUrl = "https://brandsbuilders.agency";

const seo = {
  title: "Thanks — Free 72-Hour Growth Sprint | BrandsBuilders",
  description:
    "Thanks for applying to the Free 72-Hour Growth Sprint. We’ll review your website & Google presence and send a 2-minute Loom within 72 hours.",
  canonical: `${siteUrl}/sprint/thanks`,
  ogImage: `${siteUrl}/images/og/sprint-thanks-og.jpg`,
};
---

<Layout seo={seo}>
  <!-- HERO / CONFIRMATION -->
  <section class="relative overflow-hidden bg-gradient-to-b from-slate-950 to-slate-900">
    <div class="pointer-events-none absolute inset-0" aria-hidden="true" role="presentation">
      <div class="absolute -top-24 -left-24 h-[28rem] w-[28rem] rounded-full bg-emerald-500/15 blur-3xl"></div>
      <div class="absolute -bottom-24 -right-24 h-[28rem] w-[28rem] rounded-full bg-violet-600/15 blur-3xl"></div>
    </div>

    <div class="container mx-auto px-6 relative z-10 py-16 md:py-20">
      <div class="max-w-3xl">
        <p class="inline-flex items-center gap-2 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-3 py-1.5 text-emerald-200 text-xs mb-4">
          <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
          Application received
        </p>
        <h1 class="text-4xl md:text-5xl font-black leading-tight text-white mb-4">
          Thanks — you’re in the queue
        </h1>
        <p class="text-slate-300 text-base md:text-lg">
          We’ll review your website and Google Business Profile, then send a <strong>2-minute Loom</strong> with
          before → after opportunities within <strong>72 hours</strong> (9am–9pm, daily, Alberta time).
        </p>
      </div>

      <!-- Info chips -->
      <ul class="mt-6 grid sm:grid-cols-3 gap-3 max-w-3xl">
        <li class="flex items-center gap-2 rounded-xl border border-slate-800 bg-slate-900/60 px-4 py-2 text-slate-300 text-sm">
          <svg class="w-4 h-4 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          No contracts
        </li>
        <li class="flex items-center gap-2 rounded-xl border border-slate-800 bg-slate-900/60 px-4 py-2 text-slate-300 text-sm">
          <svg class="w-4 h-4 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          Alberta only
        </li>
        <li class="flex items-center gap-2 rounded-xl border border-slate-800 bg-slate-900/60 px-4 py-2 text-slate-300 text-sm">
          <svg class="w-4 h-4 text-emerald-400" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
          Reply in 72 hours
        </li>
      </ul>
    </div>
  </section>

  <!-- NEXT STEPS -->
  <section class="py-14 bg-slate-950">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl">
        <h2 class="text-2xl md:text-3xl font-bold text-white mb-4">What happens next</h2>
        <ol class="space-y-3 text-slate-300">
          <li class="flex gap-3">
            <span class="mt-1 inline-flex h-6 w-6 items-center justify-center rounded-full bg-slate-800 text-xs">1</span>
            We check your speed, clarity and Google visibility using public data.
          </li>
          <li class="flex gap-3">
            <span class="mt-1 inline-flex h-6 w-6 items-center justify-center rounded-full bg-slate-800 text-xs">2</span>
            You receive a 2-minute Loom (before → after opportunities + quick wins).
          </li>
          <li class="flex gap-3">
            <span class="mt-1 inline-flex h-6 w-6 items-center justify-center rounded-full bg-slate-800 text-xs">3</span>
            If you want us to implement, we’ll do a short confirmation call and start your 72-hour sprint.
          </li>
        </ol>

        <div class="mt-8 grid gap-4 sm:grid-cols-2">
          <a href="/sprint" class="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-700 px-5 py-3 text-slate-200 hover:border-violet-500 hover:bg-violet-500/10 transition">
            Submit another website
          </a>
          <a href="/contact" class="inline-flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-violet-600 to-fuchsia-600 px-5 py-3 font-semibold text-white">
            Prefer to talk now?
          </a>
        </div>

        <p class="text-xs text-slate-500 mt-6">
          Tip: add <span class="text-slate-300">hello@brandsbuilders.agency</span> to your safe senders so you don’t miss the Loom.
        </p>
      </div>
    </div>
  </section>

  <!-- OPTIONAL ACCESS HINT (non-blocking) -->
  <section class="py-12 bg-slate-900">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl rounded-2xl border border-slate-800 bg-slate-900/60 p-6">
        <h3 class="text-white font-semibold mb-2">Want faster turnaround?</h3>
        <p class="text-slate-300 text-sm">
          If you already have a Google Business Profile, you can invite us as <strong>Manager</strong> (not Owner)
          at <em>Settings → People & Access</em> using <span class="text-slate-200">hello@brandsbuilders.agency</span>.
          Totally optional — you remain full owner and can remove access anytime.
        </p>
        <p class="text-slate-500 text-xs mt-3">Hours: 9am–9pm daily (Alberta). Typical Loom turnaround: 72 hours.</p>
      </div>
    </div>
  </section>

  <!-- JSON-LD -->
  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "WebPage",
          url: `${siteUrl}/sprint/thanks`,
          name: "Thanks — Free 72-Hour Growth Sprint",
          description: seo.description,
          inLanguage: "en-CA",
          breadcrumb: {
            "@type": "BreadcrumbList",
            itemListElement: [
              { "@type": "ListItem", position: 1, name: "Home", item: siteUrl },
              { "@type": "ListItem", position: 2, name: "Sprint", item: `${siteUrl}/sprint` },
              { "@type": "ListItem", position: 3, name: "Thanks", item: `${siteUrl}/sprint/thanks` }
            ]
          },
          isPartOf: { "@type": "WebSite", url: siteUrl }
        },
        {
          "@type": "Offer",
          url: `${siteUrl}/sprint`,
          price: 0,
          priceCurrency: "CAD",
          availability: "https://schema.org/InStock",
          description: "Free 72-Hour Growth Sprint application confirmed."
        }
      ]
    })}
  </script>
</Layout>
