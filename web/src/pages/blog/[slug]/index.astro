---
import Layout from '../../../layouts/Layout.astro';

/* === DATA === */
const POSTS = {
  'client-acquisition-systems-alberta': {
    title: 'Client acquisition systems for Alberta service businesses',
    excerpt:
      'How demand, funnel structure, and financial logic interact — and why most local businesses struggle to scale profitably.',
    content: `
      <p>
        Most service businesses don’t fail because of poor execution.
        They fail because they try to scale demand without understanding
        how acquisition, conversion, and unit economics interact.
      </p>

      <h2>Demand comes first</h2>
      <p>
        Before sending traffic, you must understand whether real demand
        exists in your area and how competitive that demand actually is.
      </p>

      <h2>Funnels leak quietly</h2>
      <p>
        Most funnels don’t collapse — they bleed.
        Small friction points compound and kill conversion.
      </p>

      <h2>Financial logic decides everything</h2>
      <p>
        Scaling only works when margins, acquisition cost,
        and capacity align.
      </p>
    `,
    date: '2025-01-15',
    readTime: '6 min read',
    category: 'Acquisition Systems',
  },
};

/* === REQUIRED BY ASTRO === */
export function getStaticPaths() {
  const POSTS = {
  'client-acquisition-systems-alberta': {
    title: 'Client acquisition systems for Alberta service businesses',
    excerpt:
      'How demand, funnel structure, and financial logic interact — and why most local businesses struggle to scale profitably.',
    content: `
      <p>
        Most service businesses don’t fail because of poor execution.
        They fail because they try to scale demand without understanding
        how acquisition, conversion, and unit economics interact.
      </p>

      <h2>Demand comes first</h2>
      <p>
        Before sending traffic, you must understand whether real demand
        exists in your area and how competitive that demand actually is.
      </p>

      <h2>Funnels leak quietly</h2>
      <p>
        Most funnels don’t collapse — they bleed.
        Small friction points compound and kill conversion.
      </p>

      <h2>Financial logic decides everything</h2>
      <p>
        Scaling only works when margins, acquisition cost,
        and capacity align.
      </p>
    `,
    date: '2025-01-15',
    readTime: '6 min read',
    category: 'Acquisition Systems',
  },
};
  return Object.keys(POSTS).map((slug) => ({
    params: { slug },
  }));
}

/* === PAGE LOGIC === */
const { slug } = Astro.params;
const post = POSTS[slug];

if (!post) {
  return Astro.redirect('/404');
}

const seo = {
  title: `${post.title} — BrandsBuilders`,
  description: post.excerpt,
  canonical: `https://brandsbuilders.ca/blog/${slug}`,
};
---



<Layout seo={seo}>

  <!-- HEADER -->
  <section class="bg-slate-950 py-24">
    <div class="container mx-auto px-6 max-w-3xl">

      <nav class="mb-6 text-sm text-slate-500">
        <a href="/" class="hover:text-white">Home</a>
        <span class="mx-2">/</span>
        <a href="/blog" class="hover:text-white">Insights</a>
      </nav>

      <span class="block text-sm text-slate-400 mb-4">
        {post.category} · {post.readTime}
      </span>

      <h1 class="text-4xl sm:text-5xl font-semibold text-white mb-6 leading-tight">
        {post.title}
      </h1>

      <p class="text-lg text-slate-400 leading-relaxed">
        {post.excerpt}
      </p>

    </div>
  </section>

  <!-- CONTENT -->
  <section class="bg-slate-950 pb-24">
    <div class="container mx-auto px-6 max-w-3xl">

      <article class="prose prose-invert prose-lg max-w-none">
        <div set:html={post.content} />
      </article>

      <!-- DIVIDER -->
      <div class="border-t border-slate-800 my-16"></div>

      <!-- CTA -->
      <div class="bg-slate-900 border border-slate-800 p-8 rounded-lg">
        <h3 class="text-2xl font-medium text-white mb-4">
          Want clarity before you scale?
        </h3>

        <p class="text-slate-400 mb-6 leading-relaxed">
          We offer a free client acquisition diagnostic that maps demand,
          identifies funnel leaks, and explains whether scaling
          makes financial sense — before you spend more on ads.
        </p>

        <a
          href="/contact"
          class="inline-block px-6 py-3 border border-slate-600 text-white hover:border-white transition"
        >
          Apply for Free Diagnostic →
        </a>
      </div>

    </div>
  </section>

</Layout>

<style>
  .prose h2 {
    font-size: 1.5rem;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
  }

  .prose p {
    line-height: 1.7;
  }
</style>
