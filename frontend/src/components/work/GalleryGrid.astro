---
const { images = [] } = Astro.props as { images: string[] };
---
<div class="mt-8 grid sm:grid-cols-2 gap-4">
  {images.map((src) => (
    <img src={src} alt="" loading="lazy" decoding="async" class="rounded-xl border border-white/10" />
  ))}
</div>
