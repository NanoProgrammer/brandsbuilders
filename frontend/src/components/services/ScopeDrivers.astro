---
const { items = ["Pages & features","Speed to launch","Media assets","Integrations"] } = Astro.props as { items: string[] };
---
<div class="flex flex-wrap gap-2">
  {items.map((d) => <span class="rounded-full border border-white/15 bg-white/[0.05] px-3 py-1.5 text-sm">{d}</span>)}
</div>
